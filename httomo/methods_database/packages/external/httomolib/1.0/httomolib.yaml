# httomolib version 1.0
misc:
  corr:
    remove_outlier3d:
      pattern: all
    median_filter3d:
      pattern: all
    inpainting_filter3d:
      pattern: all
  segm:
    binary_thresholding:
      pattern: all
  images:
    save_to_images:
      pattern: all
prep:
  normalize:
    normalize:
      pattern: projection
  alignment:
    distortion_correction_proj:
      pattern: projection
  phase:
    fresnel_filter:
      pattern: projection
    paganin_filter:
      pattern: projection
    retrieve_phase:
      pattern: projection
  stripe:
    remove_stripe_based_sorting:
      pattern: sinogram
    remove_stripe_ti:
      pattern: sinogram
recon:
  rotation:
    find_center_vo:
      pattern: sinogram
    find_center_360:
      pattern: sinogram
  algorithm:
    FBP_rec:
      pattern: sinogram
    SIRT_rec:
      pattern: sinogram
    CGLS_rec:
      pattern: sinogram            
    reconstruct_tomopy_astra:
      pattern: sinogram
